{
  "name": "minuteman-marketing-agent",
  "version": "1.0.0",
  "description": "Marketing automation agent for Minuteman Plumbing, Heating & Cooling — review requests, estimate follow-ups, and seasonal outbound campaigns.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "migrate": "node db/migrate.js",
    "seed": "node db/seed.js",
    "setup": "npm run migrate && npm run seed"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "node-cron": "^3.0.3",
    "nodemailer": "^6.9.16",
    "winston": "^3.17.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
